<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Connexion{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    <link rel="stylesheet" href="{{ asset('build/app.css') }}">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    {% block javascripts %}{% endblock %}

    {% block stylesheets %}
        <style>
            body {
                background-image:
                        linear-gradient(to right, rgba(0, 0, 0, 50%) 20%, rgba(0, 0, 0, 50%) 100%),
                        url("https://media.starcitizen.tools/thumb/c/cc/Pyro_Gateway%2C_Stanton_System.jpg/1920px-Pyro_Gateway%2C_Stanton_System.jpg.webp");
                background-size: cover;
                background-repeat: no-repeat, repeat;
                background-attachment: fixed;
                overflow: hidden;
                width: 100vw;
                height: 100vh;
            }
        </style>
    {% endblock %}
</head>
<body>
{% block body %}
{#    <img src="{{ asset('assets/MIS.png') }}" width="200" height="200">#}
    <div class="min-h-screen flex items-start lg:grid-cols-1 justify-center pt-20">
        <div class="w-full max-w-md shadow-lg rounded-xl p-8" style="background-color: rgba(0, 12, 45, 60%);">
            <h1 class="text-2xl font-bold mb-6 text-center text-white">MIS-Planner</h1>

            <form method="post">
                {% if error %}
                    <div class="mb-4 text-sm text-red-500 bg-red-100 p-2 rounded">
                        {{ error.messageKey|trans(error.messageData, 'security') }}
                    </div>
                {% endif %}

                {% if app.user %}
                    <div class="mb-4 text-white text-sm">
                        Vous êtes connecté en tant que {{ app.user.userIdentifier }},
                        <a href="{{ path('app_logout') }}" class="text-blue-400 hover:underline">Se déconnecter</a>
                    </div>
                {% endif %}

                <div class="space-y-4">
                    <div>
                        <label for="username" class="block text-white">Email</label>
                        <input type="email" value="{{ last_username }}" name="_username" id="username"
                               class="w-full px-4 py-2 border border-gray-300 rounded-md bg-white text-black"
                               autocomplete="email" required autofocus>
                    </div>

                    <div>
                        <label for="password" class="block text-white">Mot de passe</label>
                        <input type="password" name="_password" id="password"
                               class="w-full px-4 py-2 border border-gray-300 rounded-md bg-white text-black"
                               autocomplete="current-password" required>
                    </div>

                    <input type="hidden" name="_csrf_token" data-controller="csrf-protection"
                           value="{{ csrf_token('authenticate') }}">
                </div>

                {#
                Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                See https://symfony.com/doc/current/security/remember_me.html

                <div class="mt-4">
                    <label for="_remember_me" class="inline-flex items-center text-white text-sm">
                        <input type="checkbox" name="_remember_me" id="_remember_me" class="mr-2">
                        Se souvenir de moi
                    </label>
                </div>
                #}

                <div class="mt-6 text-center">
                    <button class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-2 rounded-md transition duration-200">
                        Se connecter
                    </button>
                </div>
            </form>

            <div class="mt-4 text-center">
                <a href="/register" class="text-blue-400 hover:underline text-sm">Créer un compte</a>
            </div>
        </div>
    </div>
{% endblock %}
</body>
</html>