{% extends 'base.html.twig' %}

{% block title %}Nouvelle équipe{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-start justify-center pt-20">
        <div class="w-full max-w-md shadow-lg rounded-xl p-8" style="background-color: #242970;">
            {% if action== 'add' %}
                <h1 class="text-2xl font-bold mb-6 text-center text-white">Créer une équipe</h1>
            {% else %}
                <h1 class="text-2xl font-bold mb-6 text-center text-white">Modifier une équipe</h1>
            {% endif %}
            {{ form_start(form) }}
            <div class="space-y-4">
                {{ form_row(form.nom, {
                    label: "Nom de l'équipe",
                    label_attr: {'class': 'text-white'},
                    'attr': {'class': 'w-full px-4 py-2 border border-gray-300 rounded-md bg-white text-black'}
                }) }}
                {{ form_row(form.membres, {
                    label: 'Membres',
                    label_attr: {'class': 'text-white'},
                    'attr': {'class': 'w-full px-4 py-2 border border-gray-300 rounded-md bg-white text-black'}
                }) }}
                {{ form_row(form.typeEquipe, {
                    label: "Type d'équipe",
                    label_attr: {'class': 'text-white'},
                    'attr': {'class': 'w-full px-4 py-2 border border-gray-300 rounded-md bg-white text-black'}
                }) }}
            </div>

            <div class="mt-6 text-center">
                {% if action== 'add' %}
                    <button class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-2 rounded-md transition duration-200">Ajouter</button>
                {% else %}
                    <button class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-2 rounded-md transition duration-200">Modifier</button>
                {% endif %}
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}