{% extends 'base.html.twig' %}

{% block title %}Réintégrer un Membre{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-start justify-center pt-20">
        <div class="w-full max-w-md shadow-lg rounded-xl p-8" style="background-color: #242970;">
            <h1 class="text-2xl font-bold mb-6 text-center text-white">Réintégrer un Membre</h1>

            {{ form_start(form) }}
            <div class="space-y-4">
                {{ form_row(form.id, {
                    label: 'Membre',
                    label_attr: {'class': 'text-white'},
                    'attr': {'class': 'w-full px-4 py-2 border border-gray-300 rounded-md bg-white text-black'}
                }) }}

            <div class="mt-6 text-center">
                <button class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-2 rounded-md transition duration-200">
                    Réintégrer
                </button>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}